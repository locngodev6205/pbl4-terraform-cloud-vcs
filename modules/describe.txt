- vpc:
  aws_vpc
  aws_subnet
  aws_internet_gateway
  aws_nat_gateway - aws_eip
  aws_route_table
  aws_route_table_association

- alb:
  aws_lb (Application Load Balancer)
  aws_lb_target_group
  aws_lb_listener
  (aws_lb_listener_rule)

- asg:
    aws_launch_template
    aws_autoscaling_group

- rds:
  aws_db_subnet_group
  aws_db_instance

- security:
  aws_security_group
  aws_security_group_rule

Internet → External ALB (Public subnets) → Web instances → Internal ALB (Private subnets) → App instances → RDS (Private subnets)
